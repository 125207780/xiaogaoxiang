package com.bonc.training.whd.mapper;

public class UserMapperSql {

	/*
	 * public String insert(User user){ String sql =
	 * "insert into whd_user(userid,user_name,user_address,user_sex, user_password) "
	 * +
	 * "values(#{userId},#{user_Name},#{user_Address},#{user_Sex},#{user_Password})";
	 * return sql; } public String delete(int userid){ String sql =
	 * "delete from whd_user where userid = #{userId}"; return sql; }
	 * 
	 * public String update(User user){ String sql = "update whd_user set" +
	 * " user_name=#{user_Name} ," + " user_password=#{user_Password} ," +
	 * " user_address=#{user_Address} ," + " user_sex=#{user_Sex} " +
	 * " where userid =#{userId} "; return sql; }
	 * 
	 * public String getById(User user){ String sql =
	 * "select * from whd_user where userid =#{userId}"; return sql; }
	 * 
	 * public String queryAll(User user){
	 * 
	 * String sql ="select * from whd_user where 1=1 ";
	 * if(user.getUser_Name()!=null && !"".equals(user.getUser_Name())){ sql +=
	 * " and user_name  like concat('%',#{user_Name},'%')"; } return sql; }
	 */

}
